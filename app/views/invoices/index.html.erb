<h1>Invoices controller, index action</h1>

<p>Edit this file in <tt>app/views/invoices/index.html.erb</tt></p>

<table>
  <tr>
  </tr>

<% for invoice in @invoices %>
  <tr>
    <td><%= link_to 'Show', url(:invoice, invoice) %></td>
    <td><%= link_to 'Edit', url(:edit_invoice,  invoice) %></td>
  </tr>
<% end %>
</table>

<%= link_to 'New', url(:new_invoice) %>
